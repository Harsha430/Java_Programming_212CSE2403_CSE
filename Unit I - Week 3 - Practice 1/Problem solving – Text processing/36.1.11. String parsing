package q11201;

public class PutInsideTag {
    public static void main(String[] args) {
        String s = args[0];
        String s1 = args[1];
        System.out.println(s.substring(0, s.length() / 2) + s1 + s.substring(s.length() / 2, s.length()));
    }
}
